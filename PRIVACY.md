# Joey Prompthub 隐私说明

更新日期：2026-07-16

## 本机保存的数据

Joey Prompthub 默认在本机保存提示词、标题、标签、备注、预览图、历史兼容数据、Seedance 模板和应用设置。图片以内容哈希命名并保存在本地资源目录，结构化数据保存在 SQLite 数据库。

API Key 单独保存在 `secure-store.json`。应用使用 Electron `safeStorage` 调用操作系统凭据保护能力；如果安全加密不可用，应用会拒绝写入密钥，而不会降级为明文存储。

Joey Prompthub 不包含遥测、广告 SDK 或后台用户分析。

## 何时会向外部服务发送数据

只有当你主动执行下列操作时，应用才会访问网络：

- 提示词优化：向所选文本 AI 服务发送提示词和必要的上下文
- 图片反推：向所选视觉 AI 服务发送图片和指令
- 连接测试/模型发现：向所选服务请求模型列表

请求会携带你为该服务配置的 API Key。服务商可能记录或保留请求，具体由其隐私政策、账号设置和合同决定。自定义端点在首次使用前需要明确确认；非本机服务只允许 HTTPS。

## 备份、删除与卸载

你可以在应用内删除提示词。卸载程序默认保留用户数据，便于重装后恢复；如需彻底移除，请在退出应用后删除 `%APPDATA%\Prompt Hub`。Joey Prompthub 为兼容旧版本继续使用这个原有目录名。

## 安全边界

渲染进程不能直接访问 Node.js、文件系统或密钥明文。网络请求、数据库和密钥操作由 Electron 主进程执行，并经过来源校验和输入验证。

如发现安全问题，请按 [SECURITY.md](SECURITY.md) 的方式私下报告。
